#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
PS1='\[\e[38;2;255;211;0m\][\u@\h \W]\$\[\e[0m\] '
fastfetch --logo /home/exec/.config/fastfetch/militech.txt
alias tor-browser='MOZ_ENABLE_WAYLAND=1 torbrowser-launcher'
export PATH="$HOME/.local/bin:$PATH"
